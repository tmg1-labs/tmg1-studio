# 現在の作業コンテキスト

最終更新: 2026-07-04（本ファイル新規作成。code-project-init で .claude 構成を初期セットアップ）

## 今やっていること
- **プロジェクト初期立ち上げ段階**。初期実装〜プレビュー再生機能まで実装済み（v0.1.0）。
  直近コミット:
  - `36acd58` プレビュー再生機能（範囲設定・境界スナップ・ループ・位置追従・範囲内シーク）
  - `de815aa` プレビュー表示サイズ選択を追加 + UI メッセージを標準語化
  - `30a9b26` TMG1 Studio 初期実装（区間ごとにモノクロ設定を変える GUI トランスコーダ）

## 一時的な制約・注意事項
- CI 未整備（GitHub Actions 等はまだ無い）。テストは手元で `cargo test`。
- ffmpeg/ffprobe は PATH 前提。実行環境に無いと probe/preview/export が失敗する。

## 次にやること
- （未定）機能追加・CI 整備・配布ビルドの動作確認など。作業に着手したら随時ここへ記録する。

## 参考
- パイプライン全体像・関連リポジトリは `CLAUDE.md` / `architecture.md` を参照。
- コーデック本体・CLI の作業状況は `tmg1-codec` / `tmg1-cli` の current-sprint.md を参照。
